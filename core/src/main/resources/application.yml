spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/daswort}
      auto-index-creation: true


daswort:
  aws:
    s3:
      bucket: ${BUCKET_NAME:daswort-files}
      region: eu-central-1
      endpoint: http://localhost:5001